# =============================================================================
# SMART CONDOMINIUM - ARCHIVO DE CONFIGURACIÓN DE ENTORNO
# =============================================================================
# Copia este archivo como .env y configura tus valores reales
# cp .env.example .env

# =============================================================================
# CONFIGURACIÓN GENERAL DE DJANGO
# =============================================================================

# Clave secreta de Django (genera una nueva para producción)
SECRET_KEY=your-secret-key-here-change-this-in-production

# Modo debug (False en producción)
DEBUG=True

# Hosts permitidos (agrega tus dominios en producción)
ALLOWED_HOSTS=localhost,127.0.0.1

# Entorno de ejecución
ENVIRONMENT=development

# =============================================================================
# BASE DE DATOS - POSTGRESQL
# =============================================================================

DB_NAME=smartcondominium_db
DB_USER=smartcondominium_user
DB_PASSWORD=your_secure_password_here
DB_HOST=localhost
DB_PORT=5432

# =============================================================================
# FIREBASE CLOUD MESSAGING (FCM HTTP v1 API)
# =============================================================================
# IMPORTANTE: Firebase ahora usa OAuth 2.0, no server keys

# ID del proyecto de Firebase (lo obtienes de Firebase Console → Project Settings)
FCM_PROJECT_ID=your-firebase-project-id-here

# Ruta ABSOLUTA al archivo JSON de credenciales del service account
# Descárgalo de: Firebase Console → Project Settings → Service Accounts → Generate Key
# ¡IMPORTANTE! Mantén este archivo seguro y no lo subas a Git
FCM_CREDENTIALS_PATH=/secure/path/to/firebase-service-account.json

# Claves VAPID para notificaciones web push
# Généralas en: Firebase Console → Project Settings → Web Push certificates
VAPID_PUBLIC_KEY=your-vapid-public-key-here
VAPID_PRIVATE_KEY=your-vapid-private-key-here
VAPID_CLAIMS_SUB=admin@smartcondominium.com

# =============================================================================
# IA Y API EXTERNAS (MÓDULO DE SEGURIDAD)
# =============================================================================

# OpenRouter API Key (para Grok de xAI)
# Obtén tu key en: https://openrouter.ai/keys
GROK_API_KEY=your-openrouter-api-key-here

# =============================================================================
# AUTENTICACIÓN JWT
# =============================================================================

# Clave secreta para JWT (puede ser diferente de SECRET_KEY)
JWT_SECRET_KEY=your-jwt-secret-key-here

# Tiempo de vida de tokens
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=60
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# =============================================================================
# CORS (Cross-Origin Resource Sharing)
# =============================================================================

# Orígenes permitidos para el frontend
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000

# =============================================================================
# EMAIL (OPCIONAL - Para notificaciones por email)
# =============================================================================

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
DEFAULT_FROM_EMAIL=noreply@smartcondominium.com

# =============================================================================
# ARCHIVOS ESTÁTICOS Y MEDIA
# =============================================================================

STATIC_URL=/static/
STATIC_ROOT=staticfiles/
MEDIA_URL=/media/
MEDIA_ROOT=media/

# =============================================================================
# DESARROLLO (OPCIONAL)
# =============================================================================

# PgAdmin (solo para desarrollo local)
PGADMIN_DEFAULT_EMAIL=admin@smartcondominium.com
PGADMIN_DEFAULT_PASSWORD=secure_admin_password

# Logging
LOG_LEVEL=INFO

# =============================================================================
# INSTRUCCIONES DE CONFIGURACIÓN
# =============================================================================
#
# 1. CONFIGURACIÓN DE FIREBASE (OBLIGATORIO):
#    - Ve a https://console.firebase.google.com/
#    - Crea un proyecto llamado "smartcondominium"
#    - Ve a Project Settings → General → Copia el Project ID
#    - Ve a Project Settings → Service Accounts → Generate new private key
#    - Descarga el archivo JSON y guárdalo en un lugar seguro
#    - Ve a Project Settings → Cloud Messaging → Web Push certificates
#    - Genera las claves VAPID
#
# 2. CONFIGURACIÓN DE IA (OPCIONAL):
#    - Ve a https://openrouter.ai/
#    - Crea una cuenta y obtén tu API key
#    - Selecciona el modelo: x-ai/grok-4-fast:free
#
# 3. BASE DE DATOS:
#    - Instala PostgreSQL
#    - Crea la base de datos y usuario
#    - Actualiza las variables DB_*
#
# 4. PARA PRODUCCIÓN:
#    - DEBUG=False
#    - SECRET_KEY: Genera una nueva segura
#    - ALLOWED_HOSTS: Agrega tus dominios
#    - FCM_CREDENTIALS_PATH: Ruta segura en el servidor
#    - Configura variables de email si usas notificaciones por email